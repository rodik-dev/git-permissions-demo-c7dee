import { Model } from '@stackbit/types';

export const object_model_1: Model = {
    name: 'object_model_1',
    type: 'object',
    fields: [
        {
            type: 'string',
            name: 'string_field'
        },
        {
            type: 'string',
            name: 'string_required',
            required: true
        }
    ]
};
